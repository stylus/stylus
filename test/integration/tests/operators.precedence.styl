body
  // 4
  foo (--- 0) or 4
  // 4
  foo --- 0 or 4
  // -5px
  foo ---5px
  // 2
  foo (not not not 5) or 2
  foo not not not 5 or 2


body
  // true
  foo not (not 5)
  foo not not 5

body
  // 5
  foo (not false) and (not false) and 5
  foo not false and not false and 5

body
  // true
  foo (!!5 == true) or (!!0 == false)
  foo !!5 == true or !!0 == false
  foo (!!5 == false) or (!!0 == false)
  foo !!5 == false or !!0 == false
  // 2
  foo 5 < 10 and !!5 and 2

body
  // true
  foo (!!true) and (!!true)
  foo !!true and !!true

body
  // 2.5
  foo 5 * 2 - 15 / 2
  foo (5 * 2) - (15 / 2)

body
  // true
  foo not (5 < 10 ? 0 : 1)
  // false
  foo not (5 > 10 ? 0 : 1)
  // wahoo
  foo not not 1 ? wahoo : fail
  // fail
  foo not 1 ? wahoo : fail

body
  foo = 'test'
  bar = 'test'
  // true
  foo (foo is defined) and (bar is defined)
  foo foo is defined and bar is defined

body
  // true
  foo not not not (bar is defined)
  foo not not not bar is defined

  // true
  foo not not (bar is a 'ident')
  foo not not bar is a 'ident'